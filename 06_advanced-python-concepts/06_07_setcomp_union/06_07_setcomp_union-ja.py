# 和集合演算子`|`を使って2つの集合を組み合わせ、
# その集合に含まれる数値が`2`より大きい場合に、
# その要素を2乗する集合内包表記を作成してください。
# コードのロジック全体を1行で記述します。
#
# 正しい出力: {16, 9, 25, 49}
#
# 集合には順序がないため、演算結果の数値の順番が
# 異なるかもしれません。

s = {1, 2, 3, 4}
t = {2, 3, 4, 5, 7}
